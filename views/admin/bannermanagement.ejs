<%-include('../layouts/admin/admin-header')  -%> 
      <!-- Breadcrumb-->
     
              <li class="breadcrumb-item active fw-light" aria-current="page">Banner</li>
            </ol>
          </nav>
        </div>
      </div>
      <!-- Page Header -->
      <header class="py-4">
        <div class="container-fluid py-2">
          <!-- <h1 class="h3 fw-normal mb-0">Users</h1> -->
          <div class="d-flex justify-content-end">
            <form action="/admin/addBannerpage" method="get">
            <button type="submit" class="btn bg-info border-0 rounded mx-1 px-4 py-2">Add Banner</button>
          </form></div>
        </div>
      </header>
      <section class="tables">   
        
         
            
            <div class="col-lg-12">
              <div class="card">
                
                <div class="card-body">
                  <div class="table-responsive">
                    <table class="table text-sm mb-0 table-striped table-hover">
                      <thead>
                        <tr>
                          <th>#</th>
                          <th>Name</th>
                          <th>Description</th>
                          <th>Link</th>
                          <th>Banner</th>
                          <th >Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% banner.forEach((banner,index)=>{ %> 
                          <tr>
                            <th scope="row"><%= index+1 %></th>
                            <td><%-banner.bannerName %> </td>
                            <td><%-banner.description %> </td>
                            <td><%-banner.action %></td>
                            <td><img src="<%=banner.image %>" width="50px"> </td>
                         <td>
                                <form action="/admin/deletebanner/<%=banner._id %>" method="post">
                                  <button type="submit" class="btn btn-link2" data-bs-toggle="tooltip" data-bs-placement="bottom" data-bs-title="Delete"  >🗑️</button></form>
                                
                                  
                             </td>
                          </tr>
                        <% }) %> 
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          
            
      </section>
      <%-include('../layouts/admin/admin-footer')  -%> 